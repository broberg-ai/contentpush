# Contentpush — kopiér til .env og udfyld. .env er gitignored; commit ALDRIG secrets.
# Alle secrets er ship-dark: mangler en værdi, degraderer appen (log-advarsel), den crasher ikke.

# Dev-server port (default 3019)
PORT=3019

# Database — Turso (cloud). Usat = lokal libSQL-fil i .data/ (dev).
# OBS: projekt-præfikset med vilje — brug IKKE generisk TURSO_DATABASE_URL
# (den har tidligere lækket fra det arvede miljø og peget på et andet projekts DB).
CONTENTPUSH_TURSO_URL=
CONTENTPUSH_TURSO_TOKEN=

# Notifikationer (F004) — Discord-webhook. Usat = notify springes over.
DISCORD_WEBHOOK_URL=

# AI-nøgler (F002+) læses af @broberg/ai-sdk selv fra env (fx MISTRAL_API_KEY).
# Tilføjes her når F002 bygges.

# AI-generering (F002) — Mistral (EU) via @broberg/ai-sdk. Usat = generate-ruten svarer 503.
MISTRAL_API_KEY=

# AI-cost-logging til upmetrics.org. Usat = cost logges ikke.
UPMETRICS_API_KEY=

# Media-storage (F003) — Cloudflare R2, EU-jurisdiction. Usat = upload svarer 503.
# Provisioneres af buddy via dns-mcp (r2_create_bucket + scoped token).
CONTENTPUSH_CONTENTPUSH_R2_ACCOUNT_ID=
CONTENTPUSH_R2_ACCESS_KEY_ID=
CONTENTPUSH_R2_SECRET_ACCESS_KEY=
CONTENTPUSH_R2_BUCKET=contentpush-media

# Cron (F004) — scoped token fra buddy; registrering kræver public URL (deploy).
CRONJOBS_API_TOKEN=
APP_PUBLIC_URL=
CRON_HOOK_SECRET=
